<% @page_title = Current.account.name %>

<div class="page page--lg">
  <div class="prose">
    <p><%= t(:account_current_name, name: Current.account.name, default: "Account name: %{name}") %></p>
  </div>
</div>
